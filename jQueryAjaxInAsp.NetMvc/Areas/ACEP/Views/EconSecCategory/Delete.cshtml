@model jQueryAjaxInAsp.NetMvc.Areas.ACEP.ViewModels.VM_TBL_ACEP_ECON_SECTOR_CATEGORY

@{ ViewBag.Title = "Delete Economic Sector Category";
                Layout = "~/Views/Shared/_Layout.cshtml"; }


<h3>
    <a href="@Url.Action("Index","EconSecCategory",new { area = "acep"})" class="btn btn-info btn-sm">
        <span class="glyphicon glyphicon-step-backward">Back To List</span>
    </a> | @ViewBag.Title
</h3>

<div class="row col-sm-8">
    <div class="panel panel-info">
        <div class="panel-heading">
            <h3 class="panel-title"> Are you sure you want to delete this? </h3>
        </div>

        <div class="panel-body">

            <table class="table table-striped">
                @*<dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.ES_CATEGORY_NAME)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.ES_CATEGORY_NAME)
                        </dd>
                    </dl>*@

                <tbody>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.ES_CATEGORY_NAME)</th>
                        <td>@Html.DisplayFor(model => model.ES_CATEGORY_NAME)</td>
                    </tr>
                </tbody>
            </table>


            @using (Html.BeginForm())
            {
@Html.AntiForgeryToken()

                <p>
                    <button type="submit" class="btn btn-danger btn-sm">
                        <span class="glyphicon glyphicon-trash">Delete</span>
                    </button>
                </p>}

        </div>
    </div>
</div>


